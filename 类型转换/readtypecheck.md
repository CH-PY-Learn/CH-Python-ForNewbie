# `type()` 函数

## 概览

为了检查变量的类型，可以使用 `type()` 函数。

## 基本用法

`type()` 中，括号内应填入要检查类型的变量。该函数返回一个表示变量类型的对象。

例如：

```python
type(1)  # 返回 <class 'int'>
```

在上述代码中，`type(1)` 返回 `<class 'int'>`，表示变量 `1` 是整数类型。