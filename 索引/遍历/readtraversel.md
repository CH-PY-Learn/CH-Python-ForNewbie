# 遍历

## 概览

当我们不清楚目标数据的索引值，或需要依次读取数据中的每一项时，应使用遍历。

## `for` 循环

遍历通常使用 `for` 循环。它会让一个循环变量不断被重新赋值为当前读取到的数据：

```python
demo_list = [...]

for i in demo_list:
    ...
```

变量 `i` 是临时变量，会依次被赋值为列表中的每一项内容。这个过程称为遍历。

## 遍历顺序与索引

遍历默认从左到右进行，顺序与正向索引一致。第一次遍历读取索引为 `0` 的数据，第 `n` 次遍历读取索引为 `n - 1` 的数据。
